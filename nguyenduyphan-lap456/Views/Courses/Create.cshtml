@model nguyenduyphan_lap456.Models.Course
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>
<form>
@using (Html.BeginForm("Create", "Courses"))
{
        @Html.AntiForgeryToken()
        <div class=<"form-group">
            @Html.LabelFor(m => m.Place)
            @Html.TextBoxFor(m => m.Place, new { @class = " form-control" })
            @Html.ValidationMessageFor(m => m.Place)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.DateTime)
            @Html.TextBoxFor(m => m.DateTime, new { @class = "form-contrl" })
            @Html.ValidationMessageFor(m => m.DateTime)
        </div>
        <div class="form-group"></div>
            @Html.LabelFor(m => m.DateTime)
            @Html.TextBoxFor(m => m.DateTime, new { @class = "form-contrl" })
            @Html.ValidationMessageFor(m => m.DateTime)

        </div>
        <div class=<"form-group">
            @Html.LabelFor(m => m.Category)
            @Html.DropDownListFor(m => m.Category,
             new SelectList(Model.Categories, "Id", "Name"), "", new { @class = " form-contrl" })
            @Html.ValidationMessageFor(m => m.Category)
        </div>
        <butto type="submit" value="Save" class="bnt bnt-primary">Save</butto>
    {
        @Scripts.Render("~/bundles/jqueryval");
    }
        <div class="form-group"></div>
        <div class="form-group"></div>
        <div class="form-group"></div>                          

}
</form>

